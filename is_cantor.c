#include <stdbool.h>
#include <stdio.h>

bool is_in_cantor(unsigned num, unsigned den, unsigned n);

int main() {
    int num = 1;
    int den = 2;
    int n = 1;

    printf("%d", is_in_cantor(1, 2, 1));
}


bool is_in_cantor(unsigned num, unsigned den, unsigned n) {
    for (int i = 0; i )
}